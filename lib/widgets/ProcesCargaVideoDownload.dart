import 'package:flutter/material.dart';

class ProcessDownloadVideos extends StatelessWidget {
  const ProcessDownloadVideos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        
        
      ],
    );
  }

  
}